{
  "name": "@unido/monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Unido - Provider-agnostic TypeScript framework for AI applications",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "test": "turbo run test",
    "cli:link": "cd packages/cli && pnpm link --global",
    "cli:unlink": "pnpm unlink --global create-unido",
    "cli:test": "node packages/cli/dist/index.js",
    "test:basic": "rm -rf test-basic-app && node packages/cli/dist/index.js test-basic-app --template basic --skip-git && cd test-basic-app && pnpm install --ignore-workspace",
    "test:weather": "rm -rf test-weather-app && node packages/cli/dist/index.js test-weather-app --template weather --skip-git && cd test-weather-app && pnpm install --ignore-workspace"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@vitest/ui": "^2.1.8",
    "c8": "^10.1.3",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
