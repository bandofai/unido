# Use shared store for the entire workspace
shamefully-hoist=true
# Prevent creating local stores
store-dir=~/.pnpm-store
# Enable strict peer dependencies (recommended for monorepos)
auto-install-peers=true
